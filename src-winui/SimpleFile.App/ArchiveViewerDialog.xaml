<ContentDialog
    x:Class="SimpleFile.App.ArchiveViewerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="using:SimpleFile.Core"
    Title="Archive Viewer"
    PrimaryButtonText="Extract All"
    SecondaryButtonText="Close"
    DefaultButton="Primary">

    <Grid Width="500" Height="400">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,12">
            <TextBlock x:Name="ArchiveNameText" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Border Background="{StaticResource SfBgSelectedBrush}" CornerRadius="4" Padding="6,2" VerticalAlignment="Center">
                <TextBlock x:Name="FormatBadgeText" FontSize="12" Foreground="{StaticResource SfTextPrimaryBrush}" />
            </Border>
        </StackPanel>

        <!-- Info Section -->
        <Grid Grid.Row="1" Margin="0,0,0,12" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Entries" FontSize="12" Foreground="{StaticResource SfTextMutedBrush}" />
                <TextBlock x:Name="EntryCountText" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Total Size" FontSize="12" Foreground="{StaticResource SfTextMutedBrush}" />
                <TextBlock x:Name="TotalSizeText" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Compressed Size" FontSize="12" Foreground="{StaticResource SfTextMutedBrush}" />
                <TextBlock x:Name="CompressedSizeText" />
            </StackPanel>
        </Grid>

        <!-- Warning InfoBar -->
        <InfoBar x:Name="WarningBar" Grid.Row="2" Severity="Warning" IsOpen="False" IsClosable="False" Margin="0,0,0,12" />

        <!-- ListView of entries -->
        <ListView x:Name="EntriesList" Grid.Row="3" BorderBrush="{StaticResource SfBorderBrush}" BorderThickness="1" CornerRadius="4">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnSpacing="8" Padding="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="32" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Icon}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                        <TextBlock Grid.Column="2" Text="{Binding SizeFormatted}" VerticalAlignment="Center" Foreground="{StaticResource SfTextMutedBrush}" HorizontalAlignment="Right" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentDialog>
